import { ProfileLogout, ProfileTitle, ProfileWrapper } from './StyledProfile'

export const ProfilePage = () => {
	return (
		<ProfileWrapper>
			<ProfileTitle>Profile page</ProfileTitle>
			<ProfileLogout>Logout</ProfileLogout>
		</ProfileWrapper>
	)
}
